# Test environment configuration

spring:
  security:
    enabled: true



# test-only logging (optional)

  logging:
    level:
      org.springframework.security: DEBUG
      com.examly.springapp: DEBUG

